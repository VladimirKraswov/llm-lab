# Workspace configuration
WORKSPACE=/workspace
ML_ENV=/opt/venv
QUANTIZE_ENV=/opt/venv
TRANSFORMERS_ENV=/opt/venv

# Service configuration
SVC_HOST=0.0.0.0
SVC_PORT=8787

# Optional: Default model to download/use
DEFAULT_BASE_MODEL=Qwen/Qwen2.5-7B-Instruct

# Web UI Origin (use * for development or specific origin for production)
WEB_UI_ORIGIN=*
